# MeltingICE.app - Environment Variables
# Copy this file to .env on your server and fill in the values

# ===========================================
# DATABASE (Required)
# ===========================================
DB_HOST=localhost
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password

# ===========================================
# ADMIN (Required)
# ===========================================
# Secret key for admin API endpoints
ADMIN_SECRET=generate_a_random_32_char_secret

# ===========================================
# EMAIL (Required for notifications)
# ===========================================
# ProtonMail SMTP token for sending emails
SMTP_PASSWORD=your_smtp_token

# ===========================================
# FRONTEND (Next.js)
# ===========================================
# These go in .env.local in the meltingice/ folder
# NEXT_PUBLIC_API_URL=https://your-domain.com/api
